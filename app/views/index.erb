<!DOCTYPE html>
<html>
  <head>
    <title>Pineterest</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style2.css">
  </head>
  <body>
    <div class="header">
    <a class="btn btn-default" id="but" href="/post" role="button">Make Pin</a>
    <p class="lead" id="hi"> Pinterest </p>    
    </div>
    <% @pins.each do |pin| %>
    <div class="pin">
      <div id="username"  class="glyphicon glyphicon-user" aria-hidden="true"><span class="test"><%= pin.user %></span></div>
      <div id="image"><%= pin.image %>
         <div><a><%= pin.link %></a></div>
      </div>
     
      <div id="content"><%= pin.description %></div>
      
      <div id="cat"><p class="lightgray">Found In</p><span class="darkgray"><%= pin.category %></span></div>
    </div>
    <% end %>
    </div>
  </body>
</html>